# RuntimeClass that pods reference via `runtimeClassName: nvidia`.
# Without this, kubelet cannot resolve the runtime name set in the
# containerd template and falls back to runc — the pod runs but the
# GPU is invisible.
apiVersion: node.k8s.io/v1
kind: RuntimeClass
metadata:
  name: nvidia
handler: nvidia
